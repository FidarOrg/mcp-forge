# {{projectName}}

An MCP (Model Context Protocol) server generated by [mcpfoundry](https://www.npmjs.com/package/mcpfoundry) from a **{{sourceType}}** source, built on [FastMCP](https://github.com/jlowin/fastmcp).

It exposes **{{tools.length}} tool(s)** over {{#if isHttp}}HTTP (streamable){{else}}stdio{{/if}}.
{{#unless isDatabase}}
> ⚙️ **Configure:** set `BASE_URL` to your upstream API before starting — e.g. `BASE_URL=https://api.example.com python server.py`. Optionally set `API_AUTH` to forward an `Authorization` header. See `.env.example`.
{{/unless}}
{{#if isSqlite}}
> ⚙️ **Configure:** set `DATABASE_PATH` to your SQLite file before starting — e.g. `DATABASE_PATH=./data.db python server.py`. See `.env.example`.
{{/if}}

## Quick start

Requires **Python 3.10+**.

```bash
python3 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
python server.py
```

List the tools (methods) this server exposes:

```bash
python server.py --list-tools
```

> Prefer packaging? `pip install -e .` also works (uses `pyproject.toml`), but
> requires a recent `pip`/`setuptools`.

{{#if isHttp}}
## Transport: HTTP

This server listens on `http://localhost:{{port}}/mcp` (override with the `PORT`
env var). Point an MCP client at that URL.
{{else}}
## Transport: stdio

This server talks JSON-RPC over stdin/stdout. Configure your MCP client to run
`python /abs/path/to/server.py`.
{{/if}}

## 🔌 Connect to Claude (Code / Desktop)

This project ships a ready-to-use **`.mcp.json`**:

```json
{
  "mcpServers": {
    "{{projectName}}": {
{{#if isHttp}}
      "type": "http",
      "url": "http://localhost:{{port}}/mcp"{{#if secure}},
      "headers": { "Authorization": "Bearer ${ZTAI_AUTH_TOKEN}" }{{/if}}
{{else}}
      "command": "python",
      "args": ["server.py"]{{#if secure}},
      "env": { "JWT_SECRET": "${JWT_SECRET}", "ZTAI_AUTH_TOKEN": "${ZTAI_AUTH_TOKEN}" }{{/if}}
{{/if}}
    }
  }
}
```

**Claude Code** — open this folder in Claude Code; it auto-detects `.mcp.json`. {{#if isHttp}}Start the server first with `python server.py`.{{else}}Use your venv's interpreter (e.g. `.venv/bin/python`) for `command` if it isn't on `PATH`.{{/if}}

**Claude Desktop** — paste the block above into `claude_desktop_config.json`{{#unless isHttp}}, replacing `server.py` with its **absolute** path (and `python` with your venv interpreter){{/unless}}, then restart the app.
{{#if secure}}
> 🔐 Secure server: set `JWT_SECRET`{{#if isHttp}} and provide your token as `ZTAI_AUTH_TOKEN`{{else}} and `ZTAI_AUTH_TOKEN`{{/if}}. Claude Code expands the `${...}` references; for Claude Desktop, paste the literal values.
{{/if}}

## Tools

{{#each tools}}
- `{{this.name}}` — {{this.description}}
{{/each}}

Each tool validates its parameters with Pydantic, then runs a real implementation
— {{#if isSqlite}}parameterised SQL against your SQLite database{{else}}{{#if isDatabase}}a `TODO` stub you can wire up{{else}}an HTTP call to `BASE_URL` via `httpx`{{/if}}{{/if}}.

{{#if secure}}
## ZTAI Security Shield (enabled)

This server was generated with `--secure`, so it enforces:

1. **JWT Guard** — {{#if isHttp}}every request must carry `Authorization: Bearer <HS256 JWT>`, verified against `JWT_SECRET` by a FastMCP middleware.{{else}}on startup it verifies a short-lived HS256 token from `ZTAI_AUTH_TOKEN` against `JWT_SECRET`. A missing/invalid token aborts boot.{{/if}}
2. **Parameter hardening** — Pydantic models reject malformed input.
3. **Deception canary** — set `ZTAI_CANARY_ID` to append a traceable marker to
   tool output (see `apply_canary` in `server.py`).

Copy `.env.example` to `.env` and fill in the values.
{{else}}
## Security

This is a standard MCP server. For zero-trust auth (JWT guard) and a deception
canary, regenerate with `mcpfoundry ... --secure`, or place it behind the ZTAI
firewall.
{{/if}}

---
Generated by mcpfoundry.
