# Environment for petstore-mcp (generated by mcpfoundry)
# Port for the HTTP transport (overrides the compiled-in default 3000).
PORT=3000
# Base URL of the upstream API the tools call (e.g. https://api.example.com).
BASE_URL=
# Optional: value sent as the `Authorization` header on every upstream call.
API_AUTH=
